[workspace]
resolver = "3"
members = [
    "apps/desktop",
    "apps/gui",
    "apps/mobile_future",
    "crates/protocol",
    "crates/core",
    "crates/crypto",
    "crates/network",
    "crates/discovery",
    "crates/input",
    "crates/clipboard",
    "crates/streaming",
    "crates/plugins",
    "crates/storage",
    "crates/telemetry",
    "crates/platform/platform-windows",
    "crates/platform/platform-linux",
    "crates/platform/platform-macos",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "MPL-2.0"
repository = "https://github.com/FehmiCitiloglu/NexKVM"
authors = ["NexKVM contributors"]

[workspace.lints.rust]
unsafe_code = "warn"
missing_debug_implementations = "warn"

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }

# Shared dependency versions. Member crates opt in with `dep.workspace = true`,
# keeping versions consistent across the monorepo.
[workspace.dependencies]
# Async runtime + utilities
tokio = { version = "1", default-features = false }
futures = "0.3"
async-trait = "0.1"

# Serialization / wire format
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
bytes = { version = "1", features = ["serde"] }

# Errors
thiserror = "1"
anyhow = "1"

# Observability
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
eframe = { version = "0.29", default-features = true }

# Identity / misc
uuid = { version = "1", features = ["v4", "serde"] }
ed25519-dalek = "2"
getrandom = "0.3"

# Hashing (SAS pairing confirmation; audited pure-Rust SHA-256)
sha2 = "0.10"

# Networking helpers
socket2 = { version = "0.5", features = ["all"] }
mdns-sd = "0.11"

# Compression (pure-Rust miniz_oxide backend; used for clipboard sync payloads)
flate2 = "1"

# Internal crates
nexkvm-protocol = { path = "crates/protocol" }
nexkvm-core = { path = "crates/core" }
nexkvm-crypto = { path = "crates/crypto" }
nexkvm-network = { path = "crates/network" }
nexkvm-discovery = { path = "crates/discovery" }
nexkvm-input = { path = "crates/input" }
nexkvm-clipboard = { path = "crates/clipboard" }
nexkvm-streaming = { path = "crates/streaming" }
nexkvm-plugins = { path = "crates/plugins" }
nexkvm-storage = { path = "crates/storage" }
nexkvm-telemetry = { path = "crates/telemetry" }
nexkvm-platform-windows = { path = "crates/platform/platform-windows" }
nexkvm-platform-linux = { path = "crates/platform/platform-linux" }
nexkvm-platform-macos = { path = "crates/platform/platform-macos" }

[profile.release]
lto = "thin"
codegen-units = 1
